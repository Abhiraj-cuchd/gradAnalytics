
const Button = ({ styles }) => {
  return (
    <button type="button" className={`py-4 px-6 bg-orange-gradient ${styles} rounded-lg font-poppins font-medium text-[18px] text-primary outline-none`}>
      Get Started Now
    </button>
  )
}

export default Button